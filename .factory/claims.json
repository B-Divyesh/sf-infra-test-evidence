[
  {
    "id": "cli-demo",
    "claim": "The CLI demo converts bundled sample data and prints paths to JUnit, JSON, and HTML outputs in a new temporary directory.",
    "where": "README demo section, /demo, and privacy page",
    "test": "npm run test:frontend -- --testNamePattern '@claim:cli-demo'",
    "sandbox": "fresh packaged CLI installation; isolated TMPDIR; no prepared input"
  },
  {
    "id": "cli-recording",
    "claim": "The landing page plays a self-hosted recording of the packaged CLI demo with a transcript and a reduced-motion view.",
    "where": "landing page and README",
    "test": "npm run qa:browser -- --grep '@claim:cli-recording'",
    "sandbox": "production site in fresh desktop and 390 px browsers; fetch the local cast; assert JUnit, JSON, and HTML output; reload with reduced motion"
  },
  {
    "id": "cli-conversion",
    "claim": "The CLI converts OpenTofu or Terraform test JSON into JUnit and a reviewer page with reviewer details and provenance.",
    "where": "landing page and README conversion section",
    "test": "npm run test:frontend -- --testNamePattern '@claim:cli-conversion'",
    "sandbox": "bundled OpenTofu sample; fresh temporary output directory"
  },
  {
    "id": "sensitive-redaction",
    "claim": "Explicit sensitive values and resource identifiers are removed from every generated artifact.",
    "where": "landing page and README conversion section",
    "test": "npm run test:frontend -- --testNamePattern '@claim:sensitive-redaction'",
    "sandbox": "packaged CLI; explicit-sensitive fixture with opaque sentinels; scan JUnit, JSON, and HTML"
  },
  {
    "id": "sensitivity-fail-closed",
    "claim": "Malformed sensitivity metadata is rejected without producing reviewer artifacts.",
    "where": "README conversion section",
    "test": "npm run test:frontend -- --testNamePattern '@claim:sensitivity-fail-closed'",
    "sandbox": "temporary malformed Terraform event stream and empty output paths"
  },
  {
    "id": "strict-validation",
    "claim": "Compact records are strictly validated and return documented process exit codes.",
    "where": "README validation section",
    "test": "npm run test:frontend -- --testNamePattern '@claim:strict-validation'",
    "sandbox": "valid sample, invalid record, and incorrect usage in a clean CLI process"
  },
  {
    "id": "event-stream-validation",
    "claim": "Event streams are rejected unless one supported summary is final, every completed run has a supported status, and every duration is non-negative.",
    "where": "README evidence safety section",
    "test": "npm run test:frontend -- --testNamePattern '@claim:event-stream-validation'",
    "sandbox": "fresh packaged CLI installation; six malformed event streams; assert exit 2, exact error, and no reviewer artifacts"
  },
  {
    "id": "run-correlation",
    "claim": "Interleaved test-plan inputs, outputs, resource changes, diagnostics, and assertion paths stay with their matching test run.",
    "where": "README evidence safety section",
    "test": "npm run test:frontend -- --testNamePattern '@claim:run-correlation'",
    "sandbox": "fresh packaged CLI installation; two deliberately interleaved runs with unique sentinels; inspect each generated case"
  },
  {
    "id": "sensitive-diagnostics",
    "claim": "A diagnostic containing sensitive data is redacted as a whole in every reviewer artifact.",
    "where": "README evidence safety section",
    "test": "npm run test:frontend -- --testNamePattern '@claim:sensitive-diagnostics'",
    "sandbox": "fresh packaged CLI installation; real-style sensitive diagnostic; scan generated JSON and HTML"
  },
  {
    "id": "conversion-only",
    "claim": "The CLI only converts existing test output; it does not invoke OpenTofu or Terraform or contact remote services.",
    "where": "landing page boundaries and README conversion section",
    "test": "npm run test:frontend -- --testNamePattern '@claim:conversion-only'",
    "sandbox": "packaged CLI in an isolated Linux process; a preload guard terminates and records any child-process or network-socket call; instrumented tofu and terraform commands are first on PATH"
  },
  {
    "id": "requested-path-writes",
    "claim": "Conversion mode writes only to standard output and the paths supplied with --junit and --evidence-dir.",
    "where": "privacy page",
    "test": "npm run test:frontend -- --testNamePattern '@claim:requested-path-writes'",
    "sandbox": "fresh packaged CLI installation; isolated working and temporary directory; preload guard rejects write-open or mkdir calls outside the exact output allowlist"
  },
  {
    "id": "artifact-private",
    "claim": "The generated reviewer page works from disk without network requests.",
    "where": "README conversion section",
    "test": "npm run qa:browser -- --grep '@claim:artifact-private'",
    "sandbox": "generated reviewer page opened from file URL with browser requests recorded"
  },
  {
    "id": "reader-private",
    "claim": "The browser reader uses no analytics, remote assets, storage, or uploads.",
    "where": "landing facts, file picker, privacy page, footer, and README",
    "test": "npm run qa:browser -- --grep '@claim:reader-private'",
    "sandbox": "fresh browser context; demo load plus local file selection; record requests, cookies, and browser storage"
  },
  {
    "id": "site-demo",
    "claim": "The first-screen demo opens a failed OpenTofu sample with redaction, provenance, and output files, then resets and exits to the real reader.",
    "where": "landing primary action, /demo banner, and .factory/demo.md",
    "test": "npm run qa:browser -- --grep '@claim:site-demo'",
    "sandbox": "fresh browser context entered from the landing primary action"
  },
  {
    "id": "browser-record-import",
    "claim": "The browser reader imports and renders a compact record, including its run, environment, checks, statuses, and validation errors.",
    "where": "landing page and README",
    "test": "npm run qa:browser -- --grep '@claim:browser-record-import'",
    "sandbox": "fresh browser context; shipped compact record followed by an invalid compact record"
  },
  {
    "id": "help-options",
    "claim": "The CLI help lists every supported option and exits successfully.",
    "where": "README strict validation section",
    "test": "npm run test:frontend -- --testNamePattern '@claim:help-options'",
    "sandbox": "fresh packaged CLI installation"
  },
  {
    "id": "json-validation-output",
    "claim": "The CLI prints a machine-readable validation result for both a compact record and an OpenTofu or Terraform event stream.",
    "where": "README strict validation section",
    "test": "npm run test:frontend -- --testNamePattern '@claim:json-validation-output'",
    "sandbox": "fresh packaged CLI installation; shipped compact record and event stream"
  },
  {
    "id": "mit-license",
    "claim": "Infra Test Evidence is free under the MIT License.",
    "where": "landing facts and README",
    "test": "npm run test:frontend -- --testNamePattern '@claim:mit-license'",
    "sandbox": "repository package metadata and LICENSE from a clean checkout"
  }
]
